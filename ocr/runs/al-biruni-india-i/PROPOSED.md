# Proposed adoption

Adopt `source/al-biruni-india-i.md` at **needs-review**.

The host OCR processed all 408 prepared pages with no API page failures. The
proposed file was rebuilt byte-for-byte from the raw OCR, has Chapters I--XLVIII
in order, resolves all five retained image references, and passes the final
diagnostic triad with exits 0/0/0. Positive controls were demonstrated for each
triad checker, the math-vocabulary census, in-page navigation stripping, and
HTML entity decoding.

Targeted readings were checked against the photographed scan, but the volume
has not been proofread page by page. This is not a `complete` claim, and the
source metadata's `ocr_status` was not changed.
