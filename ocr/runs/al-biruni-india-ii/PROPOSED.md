# Proposed text

The proposed Markdown is `source/al-biruni-india-ii.md`.

It has passed the diagnostic triad after every applied repair and in its final
form: zero lint issues, zero KaTeX failures across 100 remaining main-text math
blocks, and zero raw-LaTeX leaks. The probes were proven with a deliberately
broken positive-control file. All three image references resolve to the supplied
corpus assets, and the full asserted script chain reproduces this file byte for
byte from the untouched corpus Markdown.

The answered apparatus policy has been applied: marginal synopses, Sachau's two
annotation sections, and Index I/II are absent. This proposal is machine-checked
and suitable for adoption as `needs-review`; it is not a claim of complete
line-by-line proofreading. See `NOTES.md` for the source citations, counts, and
known limits.
