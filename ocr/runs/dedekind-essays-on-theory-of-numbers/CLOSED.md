Superseded by `dedekind-essays-on-theory-of-numbers-with-tex`, which is what was adopted.

This run was given only the publisher's PDF, and stopped rather than ship a draft
whose mathematics had ceased to exist — 0 math blocks, Greek as mojibake, 227
instances of Dedekind's set-relation symbol flattened to the digit `3`. It named
the LaTeX source that would fix it, and the re-run with that file produced 3,262
math blocks from the same text under the same instructions.

Kept because that pair is the evidence for the source-native track, and the
cleanest controlled experiment this pipeline has: one file added to `source/`,
everything else held constant. Nothing here is waiting on us.
