Converted deterministically, never dispatched — no worker, no log, no provenance.

The source was an EPUB whose markup already separated text from apparatus, so
this text was derived by a script in this directory rather than by OCR. It is
published and adopted; the dashboard reported it EMPTY only because it was
looking for a run record that was never going to exist.
