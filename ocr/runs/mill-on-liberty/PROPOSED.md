I propose `mill-on-liberty.md` as the library transcription.

It contains Mill's complete five-chapter work, dedication, epigraph, and nine
authorial footnotes; removes the signed W. L. Courtney introduction and edition
furniture; rebuilds byte-identically through `build_mill_on_liberty.py`; and
matches the supplied sibling PDF's independently extracted retained span for
all 48,271 visible tokens. The triad passes, although its zero-math result is
not evidence about word correctness. The EPUB and PDF share one Gutenberg
transcription, so printed-page review remains required and `ocr_status` was not
changed.
