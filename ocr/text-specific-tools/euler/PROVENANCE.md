# Where these scripts came from

Written by dispatched workers and copied here at adoption. The run directory under `ocr/runs/` holds the worker's own notes on each.

- `euler-elements-of-algebra` — 5 script(s) from dispatch run `euler-elements-of-algebra`, adopted 2026-08-12: `build_euler.py`, `derive-prepared.sh`, `derive.sh`, `finalize_euler.py`, `prepare_euler.py`
