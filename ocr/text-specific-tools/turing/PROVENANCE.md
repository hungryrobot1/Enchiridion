# Where these scripts came from

Written by dispatched workers and copied here at adoption. The run directory under `ocr/runs/` holds the worker's own notes on each.

- `turing-on-computable-numbers` — 6 script(s) from dispatch run `turing-on-computable-numbers`, adopted 2026-08-09: `audit_source_layout.py`, `build_turing.py`, `check_duplicate_leaves.py`, `prepare_turing.py`, `verify_triad_controls.py`, `verify_turing.py`
